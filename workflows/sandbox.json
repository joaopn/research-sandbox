{
  "name": "sandbox",
  "group": "base",
  "tags": [],
  "substrate": "docker",
  "description": "A single confined runc container (ssh, no inner docker, no agent) — a bare sandbox box. The editor is on by default; disable it with `--disable code-server`."
}
