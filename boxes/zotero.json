{
  "name": "zotero",
  "image": "base",
  "agent_default": true,
  "clone": false,
  "setup": "pip install --user 'zotero-mcp-server[pdf]==0.9.0'",
  "fields": [
    {"name": "ZOTERO_API_KEY", "label": "Zotero API key (from zotero.org/settings/keys)", "secret": true},
    {"name": "ZOTERO_LIBRARY_ID", "label": "Zotero library ID (your userID, shown on the same page)", "secret": false}
  ],
  "mcp_servers": {
    "zotero": {
      "command": "zotero-mcp",
      "env": {
        "ZOTERO_LOCAL": "false",
        "ZOTERO_LIBRARY_TYPE": "user",
        "ZOTERO_MCP_TOOLSETS": "libraries,discovery,pdf-geometry",
        "ZOTERO_API_KEY": "${ZOTERO_API_KEY}",
        "ZOTERO_LIBRARY_ID": "${ZOTERO_LIBRARY_ID}"
      }
    }
  },
  "description": "A Zotero library box: the zotero-mcp server wired to your library over the web API — search, read fulltext, manage notes and tags. Tick Browser to websearch alongside."
}
